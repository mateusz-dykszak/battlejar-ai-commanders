Execute one of the following tasks, commit and push to github when task is completed.
If no open tasks remain, analyze history/ (.log and .md files only) and add new improvement tasks below.

- [x] Analyze AGANTS.md and the client sources and setup the game client

- [x] Modify Gradle setup to copy the result jar to ./build/commander.jar

- [x] Move carrier toward center at game start — carrier stays in its spawn corner every game, giving up all map control. Use MOVE to push it toward (0|0) relative offset (i.e. hold position near center-field).

- [x] Give fighters explicit formation positions — fighters currently drift with no positional orders and never contest center lanes. MOVE each fighter to a spread formation around the carrier (e.g. offsets like 80|0, -80|0, 0|80) so they create a real screen.

- [x] Prioritize attacking the enemy carrier — current ATTACK sends fighters at the nearest enemy. Add TARGET "C" and ATTACK with the enemy carrier id so fighters focus on the win condition instead of trading with escorts.

- [x] Dock damaged fighters — no health check exists at all. Parse numeric status; DOCK any fighter below a health threshold (e.g. < 30) so it survives and reloads.

- [x] Fire missiles — FIRE_MISSILE is never called. Fire carrier missiles when an enemy carrier is within range, and release fighter missiles when fighters are close to an enemy carrier.

- [x] Deploy docked fighters — all fighters start docked and only undock when they receive an order (confirmed from game data: 0 blue fighters ever deployed while all other teams had fighters active). Send formation MOVE orders to docked fighters that are not in recovery so the carrier releases them at the game's configured rate. Track fighters we explicitly docked for damage recovery (in a Set) and skip them for a recovery window (~3 s) before redeploying.

- [x] Fix TARGET + ATTACK cooldown conflict — the 150 ms per-entity cooldown means only TARGET "M" gets sent when enemy missiles are nearby; the ATTACK that follows is always dropped. Drop the redundant ATTACK call: TARGET "M" alone is sufficient because auto-targeting fires at missiles while that filter is set.

- [x] Carrier PATROL when centered — once the carrier reaches center it sends no orders (idle) unless fighters are gone. A stationary carrier is an easy target. Send PATROL when centered so it keeps moving unpredictably while fighters are active.

- [x] Stop moving carrier to world center — confirmed from JSONL: carrier reaches center at ~6s, takes fire from 3 directions simultaneously, dies at 13–20s with only 4–10 fighters active. The one game Klaudiusz survived to 52s he was in a corner. Remove the MOVE-to-center logic; keep carrier in its spawn quadrant (border safety only). Compensate by increasing fighter formation radius from 80 to 150 so fighters naturally cover the center lanes.

- [x] Directional fighter formation — the 8-position symmetric ring wastes half the fighters facing borders or empty space. Rotate the formation to face the nearest enemy carrier: compute the direction to the enemy, then place all 8 slots in a forward-biased arc (e.g. a semicircle on the enemy-facing side).

- [x] Active missile intercept — TARGET "M" makes fighters fire at missiles already in laser range but doesn't move fighters to intercept. When an armed enemy missile is within 150 units of the carrier, find the fighter closest to the missile's path and send it a MOVE order to the missile's current position (carrier-relative).

- [x] Adaptive formation radius — the fixed 150-unit radius leaves early fighters too far to shield the carrier. Missiles enter the 80-unit danger zone at t=4s when only 2-3 fighters are deployed; at radius 150 those fighters are spread wide and miss. When fewer than 8 fighters are active, use radius 50 so early fighters cluster tightly around the carrier. Once 8+ fighters are active, switch to the normal 150-unit directional arc. (Analysis: every 13s death shares the pattern of 2 missiles inside 80 units by t=4-5s with only 2-3 fighters out.)

- [x] Carrier evasion from incoming missiles — the carrier only PATROLs while fighters are active, making no attempt to dodge. Missiles are guided (velocity always points at their target) so their (vx, vy) reveals if they are heading straight for us. When an armed enemy missile is within 200 units and its velocity vector points toward our carrier (dot product check), issue MOVE to the carrier perpendicular to the missile's velocity. This should take priority above FIRE_MISSILE but below border safety. (Analysis: in the 13s-death games, missiles entered the 80-unit zone while the carrier was stationary under PATROL; a lateral dodge of ~60 units would make many of those miss entirely.)

- [x] Carrier aggression after first kill — after eliminating at least one enemy carrier (track initial enemy count vs current), switch from PATROL to closing in on the nearest remaining enemy when we have 12+ active fighters. In the one game we survived (132s), we dominated Red for 70+ seconds but never pressed the attack; Red endured because we never moved in. MOVE the carrier toward the nearest enemy carrier when dominant instead of idling under PATROL.

- [x] Full 360° ring for early formation — buildFormation currently uses a 180° arc (half-circle) for both tight and wide radii. When fewer than 8 fighters are active, multiple enemies attack from different angles simultaneously; the 180° arc leaves the flanks and rear of the carrier unguarded. Switch to a full 360° ring at radius 50 for the early phase so fighters provide all-around protection. Once 8+ fighters are active, keep the 180° directional arc at radius 150 for forward pressure. (Analysis: in all winning games, fighter count dips to 1–3 at t=8–9s as early fighters die shielding the carrier — the tight ring is working, but only covers one direction.)

- [x] Focus-fire weakest enemy carrier — fighters and carrier push currently target the nearest enemy carrier. A carrier at low HP (≤ 300 / 1000) is a kill opportunity that directly reduces incoming fire. Change target selection to prefer the lowest-HP enemy carrier within 350 units; fall back to nearest if none are wounded. Eliminating carriers faster should improve our own survival by reducing the number of enemies shooting at us.

- [x] Wider missile detection thresholds — TARGET "M" fires at missiles within 200 units; physical intercept triggers within 150 units. At ~33 units/second a missile at 200 units hits in ~6 seconds — barely enough time for a fighter to move. Increase TARGET "M" to 300 units and intercept to 250 units for more lead time. (Analysis: max early-game missile count within 200 units reached 9 in some games; earlier detection would give fighters more frames to engage.)

- [x] Prioritize carrier FIRE_MISSILE above dodge — the carrier dodge order (priority 2 in the carrier chain) fires every 150 ms whenever a missile is within 200 units heading our way, which is almost always from t=3s onward. FIRE_MISSILE sits at priority 3, meaning the carrier almost never fires its own missiles. Move FIRE_MISSILE to priority 1 (before border, dodge, and all movement orders): if missiles > 0 and an enemy carrier is visible, fire immediately; the dodge activates on the very next 150 ms tick. (Analysis: in all threats CSVs, carrier HP drops from t=11s onward while armed_missiles_within_200 is always ≥ 1 — the carrier is dodging instead of contributing DPS, leaving enemy carriers free to reload and refire indefinitely.)

- [x] Per-fighter missile proximity for TARGET "M" defense — currently, when ANY armed missile is within 300 units of our carrier, ALL fighters switch to TARGET "M" defensive mode. Missiles are present from t=5s onward in nearly every game, so ALL fighters spend the entire game in laser-defense mode and never attack. Replace the shared enemyMissilesNearby flag with a per-fighter check: a fighter switches to TARGET "M" only when an armed enemy missile is within 150 units of THAT fighter (not of the carrier). Fighters further away continue issuing ATTACK orders on the target carrier. This splits the squad into active defenders (near missiles) and attackers (pressing the enemy), so offensive pressure on enemy carriers never fully stops. (Analysis: every game where we died at peak ≥ 12 fighters had 3 enemies alive simultaneously; the only way to reduce 3-way fire is to kill enemies faster, which requires fighters to keep attacking even during missile waves.)

- [x] Shrink physical intercept range from 250 to 80 units — sending fighters to physically move to missile positions at 250 units (7.5 seconds away) pulls most of the formation away from the carrier. By the time the fighter arrives the missile has moved, so the intercept rarely succeeds. Shrink MISSILE_INTERCEPT_RANGE to 80 units (missiles that are about to enter the carrier's danger zone in ~2 s). Rely on TARGET "M" lasers for missiles at 80–300 units; reserve physical interception only for missiles deep inside the defensive ring. (Analysis: threats CSVs show armed_missiles_within_80 reaching 3–6 in death games while armed_missiles_within_200 reaches 5–9; many of the 250-unit intercept assignments are chasing missiles that fighters can't catch before they hit anyway.)

- [x] Fighter FIRE_MISSILE before TARGET "M" — when a fighter has missiles loaded AND the enemy carrier is within FIGHTER_MISSILE_RANGE (150 units), fire the missile before switching to laser defense. Currently FIRE_MISSILE is priority 6 while TARGET "M" is priority 4, so fighters never fire their missiles at a close enemy carrier if any missile is also nearby. The enemy carrier approaching to 79–130 units is a kill opportunity: burst missile fire can eliminate it faster than lasers, stopping the missile source entirely. Reorder: check `fighter.missiles() > 0 && nearestEnemyCarrier within 150` at priority 4, before the per-fighter TARGET "M" check. (Analysis: threats CSVs show enemy carriers reach 79–130 units in most games; fighter missiles at that range would hit immediately. In 132s survived game, enemy carrier reached 79 units at t=14 — that's point-blank missile range.)

- [x] Increase FORMATION_THRESHOLD to reduce slot-chasing — the carrier dodge order moves all 8 formation slots by 60 units every 150 ms. FORMATION_THRESHOLD = 50 means a fighter 51 units from its slot is "out of formation" and spends the next tick repositioning instead of attacking. With frequent dodges, fighters chase slots continuously. Increase FORMATION_THRESHOLD from 50 to 100: fighters within 100 units of their slot count as in formation and can attack. This keeps the spread formation (slots still define positions) while reducing time lost to micro-repositioning. (Analysis: carrier dodges every 150 ms when missiles are within 200 units — true from t=3s onward — displacing all slots by 60 units and putting all fighters "out of formation" every cycle.)

- [x] Carrier kite — maintain distance when outnumbered

- [x] Disable carrier kite in 1v1 and lower aggression threshold — when exactly 1 enemy carrier remains, the carrier kite is counterproductive: it creates a kite loop (enemy approaches 148 units → we retreat → enemy approaches again → repeat) that prevents us from ever dealing decisive damage. Evidence: bd167898 shows ec oscillating 148-174 for 15 seconds of 1v1 while carrier hp slowly fell. Also lower AGGRESSION_FIGHTER_THRESHOLD from 12 to 6 when only 1 enemy remains: during every 1v1 we had 5-9 fighters (never 12), so aggression never activated. Passive PATROL loses every 1v1 in these 9 games. Changes: (1) kite condition adds `&& liveEnemyCarriers.size() > 1`; (2) aggression threshold uses `liveEnemyCarriers.size() == 1 ? AGGRESSION_FIGHTER_THRESHOLD / 2 : AGGRESSION_FIGHTER_THRESHOLD`.

- [x] Attack enemy fighters inside our formation zone — when an enemy fighter is within FORMATION_RADIUS_TIGHT (50 units) of our carrier, prioritize attacking it over the distant enemy carrier. Evidence: e4cbed4f 1v1 phase shows hp=890 stable for 20s then sudden 305 HP drop in 1s with m80=0 (no missile — pure fighter laser damage) as our fighter count dropped 6→2. Enemy fighters rush inside our formation ring and laser our carrier from point-blank range. Add: after per-fighter TARGET "M" check but before MOVE-to-slot and carrier ATTACK, find the nearest enemy fighter within FORMATION_RADIUS_TIGHT of our carrier and send ATTACK on it. This keeps our fighters clearing the close-range threat before attacking the distant carrier.

- [x] Carrier also attacks in 1v1 — currently carrier only fires missiles (priority 1) and then dodges/PAT ROLs. In 1v1, the enemy carrier is often at 150-260 units — potentially within laser/ATTACK range. When `liveEnemyCarriers.size() == 1` and no missiles to fire and no dodge needed, issue ATTACK nearestEnemyCarrier instead of PATROL. The carrier's own lasers add supplementary DPS and the carrier auto-moves toward the enemy, naturally closing range without a separate push order. (Analysis: in 33.6s of 1v1 in e4cbed4f, carrier probably just PATROLed the whole time while enemy carrier sat at 250+ units untouched by carrier lasers.) — when an enemy carrier is within 150 units AND we have fewer than 8 active fighters, issue MOVE away from the nearest enemy carrier (opposite direction, 80 units, border-safe). Add this as a new priority between dodge and PATROL. The carrier approach is universal (always from 180 to ~80-130 units by t=15s) but is survivable when enemies fight each other; when they don't divert and we have a small fighter screen, point-blank range overwhelms us. Kiting adds ~2–3 seconds of missile approach time per dodge-vs-kite cycle. (Analysis: fc7c192f survived with ec=79 because the close enemy then pivoted to fight another; death games show ec=93-130 at death with no diversion. With <8 fighters, we cannot intercept fast enough at those ranges.)

- [x] Scale formation radius to ec/2 in 1v1 — when exactly 1 enemy carrier remains AND it is within FORMATION_RADIUS_WIDE (150), use `max(FORMATION_RADIUS_TIGHT, min(FORMATION_RADIUS_WIDE, nearestEnemyDist/2))` as the effective wide radius instead of a fixed 150. This keeps the forward-most fighters halfway between the two carriers instead of past the enemy, ensuring they block lasers and intercept missiles from the correct direction. Evidence: 91f49fd1 shows zero damage while ec ≥ 149 (t=20–40), then 50–105 HP/s loss as ec falls to 137–85 (t=41–54) with m80=0 — pure carrier laser damage, not missiles. Overshoot = max(0, 150-ec) perfectly predicts when sustained damage begins. 9a3bf322 confirms extremes: ec=23, overshoot=127, 113 HP/s damage. 6493299c: 279 HP drop in 1s at ec=107 (os=43) and 500 HP drop in 1s at ec=92 (os=58), both with m80=0 — fighter lasers from past the enemy carrier.

- [x] Minimum carrier separation in 1v1 — aggression push (MOVE toward enemy) combined with the enemy ATTACK auto-move drives both carriers together until they nearly collide (ec=23 in 9a3bf322). Add a floor: when `liveEnemyCarriers.size()==1 && nearestEnemyCarrierDist < 80`, skip the aggression push (and ATTACK) and instead issue MOVE away from the enemy carrier by 60 units (border-safe). This prevents collision, keeps ec at 80+ where even the tight formation (radius=50) doesn't overshoot, and avoids the auto-approach feedback loop where both carriers simultaneously ATTACK each other.

- [x] Expand intruding-fighter ATTACK range from 50 to 100 units — current threshold only catches enemy fighters touching the tight ring. In 1v1, the enemy carrier pushes to 80–110 units of our carrier; its escort fighters spread 30–60 units ahead, landing 50–80 units from our carrier — outside our 50-unit detection. This lets them laser our carrier freely. Change the intruding fighter check from `distance(e, myCarrier) < FORMATION_RADIUS_TIGHT` to `distance(e, myCarrier) < 100`. This ensures we attack escort fighters in the close-approach corridor before they concentrate laser fire. Evidence: 6493299c shows 279 HP in 1s and 500 HP in 1s at ec=92–107 with m80=0; the source must be both the enemy carrier AND its fighters at those ranges.

- [x] Per-fighter independent intruder targeting — the shared `intrudingEnemyFighter` variable picks one enemy fighter (nearest to our carrier) and ALL our fighters attack it, leaving every other intruder free to laser us. With ef_near_100 reaching 10–14 by t=12–16, this means 9–13 uncontested enemy fighters deal full DPS on the carrier while all 8 of ours pile on one target. Replace the shared variable with a per-fighter check: inside the fighter loop, each fighter finds the nearest enemy fighter within 60 units OF THAT FIGHTER (not of the carrier) and attacks it. This distributes our fighters across multiple intruders simultaneously — 8 fighters engaging 8 different targets instead of all 8 on 1. Evidence: d9f9a90a at t=12, ef_near=6 and hp dropped 350 in 1s (all fighters on 1 intruder; 5 others uncontested). 53503db6 at t=18–19, ef_near=10–11, hp dropped 100–155/s with m80=0.

- [x] Raise KILL_FOCUS_HP from 300 to 600 — our 3 opening carrier missiles deal ~500 damage to the nearest enemy (seen in all_carriers: enemy drops 1000→500 at t=8 in most games). With KILL_FOCUS_HP=300, this carrier at 500 HP does not qualify for kill-focus and fighters may scatter to closer threats. Raising to 600 ensures fighters and carrier immediately concentrate all DPS on the 500-HP enemy right after the missile barrage, finishing the kill by t=12–13 instead of t=14–16. Evidence: first_kill_s correlates directly with survival — all 6 survived games have first_kill ≤ 18.6s. The 3 wins have first kills at t=14.4, 15.6, and 18.4s. Faster first kill = fewer missiles and fighters from that direction for the rest of the game.

- [x] Early carrier push on wounded enemy before first kill — carrier MOVE-toward-enemy only activates after `hasKilledEnemy`. In the critical t=8–15 window, the carrier PATROLs while the first kill sits at 500 HP waiting to die. Add: when `!hasKilledEnemy && nearestEnemyCarrier != null && health(nearestEnemyCarrier) <= KILL_FOCUS_HP && myFighters.size() >= 6 && liveEnemyCarriers.size() > 1`, issue MOVE toward the wounded carrier (same CARRIER_PUSH_DISTANCE). Closing faster brings fighters within closer laser range and ends the kill 2–4s earlier. This condition is safe: it only fires when we have ≥ 6 fighters as a screen and the enemy is already wounded (not a cold-push). Evidence: in f650d958 (WIN), enemy was at 500 HP at t=8 and died at t=14.4 from fighter DPS — if our carrier closed 80 units sooner our fighters would have been within tighter range and the kill would come at t=11–12, before the second wave of enemy missiles.

- [x] 360° formation in multi-enemy phase — the 180° directional arc (used at ≥ 8 fighters) faces the nearest enemy carrier and leaves the rear 180° of our carrier completely unguarded. In multi-enemy games (live_enemy_carriers > 1), the other 2 enemies' fighters approach from the flanks and rear of the arc. Analysis of 42 multi-enemy deaths (peak > 0): ef_near_100 peak median = 16, range 3–28 — enemy fighters swarm our carrier from all directions simultaneously. Fix: use 360° ring at FORMATION_RADIUS_WIDE when `liveEnemyCarriers.size() > 1`, regardless of fighter count. Reserve the 180° directional arc for 1v1 only, where the single threat direction is known. One-line change: `formArc = (expanded && liveEnemyCarriers.size() == 1) ? PI : 2*PI`.

- [x] Raise FIGHTER_INTRUDER_RANGE from 60 to 120 — each fighter attacks the nearest enemy fighter within 60 units of itself. Fighter laser range is 150 units. In multi-enemy deaths, ef_near_100 reaches 12–28 with enemy fighters at 61–150 units from formation fighters; these are ignored in favour of attacking the distant carrier. Raising to 120 (= ~80% of laser range) means formation fighters preferentially engage nearby enemy fighters they can actually hit, rather than always defaulting to carrier ATTACK when intruders aren't within 60 units. This reduces ef_near by distributing fighter DPS against the swarm at realistic engagement distance. Risk: 120 might over-prioritise fighter-vs-fighter combat and reduce carrier DPS — verify from game data after implementing.

- [x] Raise DOCK_HEALTH_THRESHOLD from 3 to 5 — fighters dock when HP ≤ 3 (30% of 10). At HP 4 a fighter stays in combat; one laser hit from an enemy kills it permanently. Raising to 5 (50%) recovers fighters earlier, avoiding permanent loss at the cost of 3s docked per retreat. Over a 40–60s game, earlier recovery means more total fighter-lives and a larger effective screen. Evidence: 42 multi-enemy deaths all show ef_near ≥ fighters-peak, meaning enemy fighters outnumber ours locally — any improvement in own-fighter survival rate directly helps close that gap.

- [x] Revert DOCK_HEALTH_THRESHOLD from 5 back to 3 — deployment curves from 9 new games show the docked count RISING mid-fight (e.g. 24→26 at t=11–12 in 4a7bc686, 23→25 in 73214061). Fighters retreating to dock at 50% HP leave only 2–5 active vs 40+ enemy fighters, collapsing the defence faster than the threshold saves any fighter permanently. The 3-second recovery window means 3–4 fighters are simultaneously absent at the worst moment. Reverting to threshold 3 keeps more fighters fighting and reduces simultaneous dock pressure. Evidence: ab0d5bf9 reached only 1 active fighter at t=9 (25 docked) before the carrier was overwhelmed.

- [x] Fix intruder targeting: gate on carrier proximity, not fighter proximity — with FIGHTER_INTRUDER_RANGE=120 and 30–40 enemy fighters active by t=12, there is almost always an enemy fighter within 120 units of every formation fighter. This locks ALL fighters in intruder-attack mode so no fighter ever issues ATTACK on the enemy carrier — the kill-focus logic and pre-kill push are completely bypassed. First kills in new multi(3) games are at 17–42s (vs 14–20s needed to survive). Fix: change the intruder gate from `distance(e, fighter) < FIGHTER_INTRUDER_RANGE` to `distance(e, myCarrier) < FORMATION_RADIUS_TIGHT * 1.5f` (= 75 units of carrier). Keep per-fighter nearest selection for distribution. This means fighters only enter defensive mode when enemy fighters have penetrated the inner ring (~75 units from carrier), while fighters far out in the 150-unit ring continue attacking the carrier. Introduce a new constant `FIGHTER_INTRUDER_CARRIER_RANGE = 75f` and replace the distance check accordingly.

- [x] Reduce RECOVERY_MS from 3000 to 1500 — even after reverting the dock threshold, the 3-second recovery window is long relative to a 15–30s game. Halving it to 1500ms means docked-for-recovery fighters return to the fight in 1.5s instead of 3s, reducing the simultaneous absence window. The underlying fighter HP regeneration (if any) still has time to partially restore, but fighters re-enter play twice as fast. This compounds with the threshold revert: fewer fighters dock at any moment AND those that do return sooner.

- [x] Fix post-kill kite dead zone in multi-enemy — after the first kill (`hasKilledEnemy=true`), the kite branch (lines 265-278 of ClaudeCommander.java) is unreachable: it sits in a later `else if` that is never reached when `hasKilledEnemy=true`. When we have <12 fighters in multi-enemy after the kill, we PATROL while the enemy walks in unchallenged. Evidence: c42c21ca after kill at t=24.44 — ec drops 63→20 while we PATROL; carrier is at 63 units from enemy immediately post-kill with live_ec=2 and active=6 fighters. Fix: inside the post-kill `else { PATROL }` block (the current fallback at line 263), add: `if (nearestEnemyCarrier != null && distance(myCarrier, nearestEnemyCarrier) < CARRIER_KITE_RANGE && myFighters.size() < CARRIER_KITE_FIGHTER_MAX) { kite } else { PATROL }`. This reactivates kiting for the post-kill multi-enemy phase when the fighter screen is thin.

- [x] Lower 1v1 push threshold from 6 to 4 fighters — the current 1v1 effectiveThreshold = AGGRESSION_FIGHTER_THRESHOLD/2 = 6. When we have 4–5 fighters in 1v1 we fall back to carrier ATTACK (auto-move), which is slower than explicit MOVE push at closing range on a kiting enemy. Evidence: ca7cac1a shows active=1–5 fighters throughout 33s of 1v1 while VIOLET kites to 185–267 units, taking zero damage for the entire fight. With threshold=4, we push (MOVE 80 units) with 4+ fighters instead of waiting for 6. Change: `liveEnemyCarriers.size() == 1 ? AGGRESSION_FIGHTER_THRESHOLD / 3 : AGGRESSION_FIGHTER_THRESHOLD` (= 4 instead of 6 in 1v1). This applies to the effectiveThreshold calculation at line 250.

- [x] Raise FIGHTER_INTRUDER_CARRIER_RANGE to 100 in 1v1

- [x] Increase 1v1 carrier push distance from 80 to 400 units — the 80-unit push target tracks ~80 units ahead of the enemy; if the enemy retreats at carrier speed, our net closure rate = 0. Evidence: 9a3bf322 shows ec=218→127 over 48 seconds (1.9 units/sec) while we deal ZERO damage to the enemy (ec_hp=194 for 20 straight seconds) because our fighters at formation radius 100 can barely reach at 200+ units. With a 400-unit target the carrier aims PAST the enemy and always moves at maximum speed toward them; the CARRIER_MIN_SEPARATION_1V1 floor at 80 units catches closure and prevents collision, settling combat at ec ≈ 80-100 units where the tight formation (radius 50) puts ALL 8 fighters within 50 units of the enemy. Change: when `liveEnemyCarriers.size() == 1`, use `CARRIER_PUSH_DISTANCE * 5` in the push MOVE calculation (multiply dist factor for the 1v1 branch only). Add constant `CARRIER_PUSH_DISTANCE_1V1 = CARRIER_PUSH_DISTANCE * 5 = 400f`. — in multi-enemy, the 75-unit carrier-relative gate prevents all fighters from locking into defensive mode when 30+ enemies fill the 100-unit band (task 80 fix). In 1v1, the total enemy count is lower and enemy fighters cluster near our carrier: ca7cac1a shows ef_near_100 building to 17 while we take sustained -50 HP/s with m80=0 — those fighters at 76–100 units from our carrier fire unchallenged (outside the 75-unit gate). Fix: compute the intruder range dynamically — use 100f when `liveEnemyCarriers.size()==1`, keep 75f in multi-enemy. Replace the constant in the per-fighter intruder filter with `float intruderRange = liveEnemyCarriers.size() == 1 ? 100f : FIGHTER_INTRUDER_CARRIER_RANGE`. Pass `liveEnemyCarriers` into scope (already available) and modify the filter on line 173.

- [x] Raise KILL_FOCUS_HP from 600 to 750

- [x] Fix fighter FIRE_MISSILE direction — no-arg FIRE_MISSILE fires toward the fighter's current facing direction. Fighters constantly move back to formation slots around our carrier, so their facing is often toward the carrier. This caused fighter missiles to hit our own carrier across 84 games (0 wins). Fix: pass explicit (fdx, fdy) direction from fighter to enemy carrier so missiles always fly away from our carrier toward the enemy. — opening carrier missiles consistently deal exactly 250 HP (confirmed across 30 games: avg post-barrage hp=708, nearly always 1000→750). With KILL_FOCUS_HP=600, the post-barrage target at 750 HP does NOT enter kill-focus; fighters scatter to other targets for the next 8–12 seconds until the enemy dips below 600 from attrition. Evidence: 011fc311 died with ORANGE at 3 HP because kill-focus didn't activate until t=16 (ORANGE at 580 HP), wasting 8 seconds of concentrated fire. Raising to 750 ensures kill-focus activates immediately after the missile barrage at t=8, so fighters concentrate on the 750-HP carrier and finish the kill by t=11–12 instead of t=16+. Change: `private static final int KILL_FOCUS_HP = 750;`.

- [x] Filter threatening missiles by direction, not by color — ownership does not matter; what matters is whether a missile is heading toward our carrier. A missile moving away from us (our own outgoing, or an enemy missile targeting a different player) is harmless and should be ignored. A missile that loses its target can change direction rapidly and become dangerous regardless of who fired it. Replace the color-based filter `!myColor.name().equals(e.color())` in `armedEnemyMissiles` with a direction check: keep only missiles whose velocity vector has a positive dot product with the vector from the missile to our carrier (`missile.vx*(carrier.px-missile.px) + missile.vy*(carrier.py-missile.py) > 0`). This same dot product check already exists correctly in `computeCarrierDodge` — also remove the color filter there so it handles redirected own missiles too. With the current color filter, our own outgoing carrier missiles (color="NONE" ≠ myColor) are treated as threats for the first ~9 seconds: ALL fighters switch to TARGET "M" and laser our own missiles, and `buildInterceptAssignments` sends fighters to chase them. Evidence: d3eeafc5 (GREEN, died t=15.71s): nearest_ec_hp_at_t8=1000, nearest_ec_hp_at_t12=950 — 6-8 fighters active for 12 seconds with near-zero damage to enemy carriers.

- [x] Improve fighter offensive output after missile-ownership fix — superseded by fix below; root cause was the FIRE_MISSILE heading bug, not TARGET "M" overuse.

- [x] Fix player username: register as "Klaudiusz" so stats.py can identify our carrier — player registered with `new Player(null, null, null)`; stats.py looks for username=="Klaudiusz" at line 88. All recent games show `our_color: null` meaning we are flying completely blind, unable to track own HP, fighter count, or damage dealt. Fix: `new Player(null, null, "Klaudiusz")` in Main.java.

- [x] Fix fighter FIRE_MISSILE heading: check velocity alignment before firing — fighters fire along their velocity vector (direction arg is carriers-only). TURN_XY rotates velocity gradually, not instantly; the one-tick preFireSet approach still fired while the fighter was mid-turn. Fix: compute cosine between fighter velocity and direction-to-enemy-carrier each tick; if cosAngle > 0.7 (~45°) fire FIRE_MISSILE, otherwise re-issue TURN_XY and wait. Missiles lock on carriers in their forward cone; if none found they home to the nearest carrier (ours) regardless of distance. Spacecraft cannot be stationary so velocity is always a valid heading indicator.

- [x] Increase FORMATION_RADIUS_TIGHT from 50 to 80 — fighters at 50-unit radius physically overlap the carrier body, triggering auto-dock from collision. Additionally, 8 fighters in a 50-unit ring are only ~39 units apart; at typical fighter hitbox sizes they constantly interfere with each other. 80 units gives ~63 units between adjacent fighters and clears the carrier's physical boundary. This is confirmed by user observation: "fighters constantly docking to carrier" with no explicit DOCK order being issued by our code.

- [x] Verify fighter offensive output after heading fix — 3 new games with our_color identified: nearest_ec_hp_at_t8=1000 in all 3, nearest_ec_hp_at_t12=950-1000. Fighters deployed (peak 8-15) but dealt zero laser damage for 8+ seconds. Root cause: TURN_XY re-issued every 150 ms prevented it from ever completing (HOWTO: "Sending TURN_XY too frequently can cause neither your commands nor the automated ones to complete"). Fighters stuck in missile-alignment branch, never reaching ATTACK. Fix: move missile fire check to AFTER the formation/TARGET-M chain; fire only when fighter is already in formation AND cosAngle > 0.5 (naturally aligned from prior ATTACK movement). Fighters now always fall through to ATTACK when not aligned, delivering continuous laser damage.

- [x] Verify fighters now deal laser damage — 3 games after TURN_XY fix: nearest_ec_hp_at_t8=750 (< 950 ✓), nearest_ec_hp_at_t12=750 (< 900 ✓). The 250 HP drop at t=8 is carrier missiles landing; ec_hp stays at 750 from t=8–12 with 3–5 active fighters. Root cause discovered: at spawn (y=198, worldHeight=216) only 2 of 8 formation slots (radius=80) are in the safe zone (y < 166). The other 6 slots are at y > 166 or x > 334, so those fighters trigger `isNearBorder` and get `MOVE("0|0")` instead of ATTACK. Fix: clamp slot absolute positions to the safe zone before computing the relative MOVE offset and the inFormation check.

- [x] Clamp formation slots to safe zone — at carrier spawn only 2/8 slots are within safe margins (border_margin+safe_inset=80). Fighters assigned to out-of-bounds slots spend all time on MOVE("0|0"). Fix: compute clamped absolute slot position (max(safeInner, min(worldWidth-safeInner, carrier.px+offset[0])), same for y), derive relative offset from that. Applied to both the active-fighter loop and the docked-fighter deploy loop. Verified: compiles cleanly.

- [x] Verify formation clamping delivers fighter laser damage — analyzed ae9fd11d (our_color=WHITE, 1 game post-clamping-fix): nearest_ec_hp_at_t8=750, at_t12=750 — still zero fighter laser damage. Root cause: FIGHTER_LASER_RANGE=150 too large; forward fighters (80 units from carrier) trigger TARGET "M" defense when missiles are at 100-140 units from them (81-141 from carrier). Fixed: renamed to FIGHTER_MISSILE_DEFENSE_RANGE and reduced from 150→80. At 80, the per-fighter defense range matches MISSILE_INTERCEPT_RANGE — only missiles within 80 of the fighter trigger defensive mode, leaving forward fighters free to ATTACK.

- [x] Verify FIGHTER_MISSILE_DEFENSE_RANGE=80 improves laser damage — 3 games verified (7a0a4631, ae9fd11d, 84d55a18). ec_hp_t8=750 and ec_hp_t12=750 in all 3: fighter laser damage does NOT show at t=8-12 because ec_dist is still 120-151 at those moments with only 1-5 fighters deployed. Damage DOES start at t=13-14 when ec_dist reaches 107-116 (within ~115 laser range), confirmed in both games. The FIGHTER_MISSILE_DEFENSE_RANGE fix is working — defense mode no longer blocks ATTACK. Root cause of no early damage: ec_dist > 195 (carrier+80 formation > 115 laser range) until t=13. Win rate 1/3. First kill: 14.7s, 15.54s, 23.24s. Fix: carrier push clamping (see next task).

- [x] Investigate 1v1 stall: root cause identified and fixed. The carrier push command (CARRIER_PUSH_DISTANCE_1V1=400) computed a relative MOVE offset of (-398, -32) which corresponds to absolute target (-93, 130) — outside the world. The game server silently drops out-of-bounds MOVE commands; the carrier continued PATROLing from the multi-enemy phase. Evidence: WHITE at (305, 162), ORANGE at (67, 143), push target = (-93, 130) clipped to world left border. Additionally, carrier y=167 > worldHeight-BORDER_MARGIN=166 triggers border avoidance on ~30% of ticks, consuming the cooldown slot that the push order needs. Fix: clampedCarrierMove() helper computes absolute target then clamps to safe zone [margin, worldDim-margin] before deriving relative offset. Applied to both pre-kill push and post-kill push. Now sends MOVE(-226, -32) instead of MOVE(-398, -32), which is within world bounds and accepted by the server.

- [x] Verify carrier push clamping fix works in 1v1 — only 2 Klaudiusz games available (2beebbc6, 46c1112f); 1 reached 1v1 (2beebbc6, entered at hp=80 — already dying). In that 1v1: ec_hp dropped 1000→700 (fighter damage ✓), ec_dist increased 211→227 (push not closing gap, enemy kiting faster). Not a clean test (80 HP entry). Need more 1v1 data. Fix found: postprocessing scripts had "BLUE" fallback causing analysis of wrong carrier in non-Klaudiusz games — fixed in postprocess.sh, threats.py, all_carriers.py, deployment.py. Also note: history/ contains games from other players; scripts now skip games where Klaudiusz is absent.

- [ ] Investigate 1v1 ec_dist stall after clamping fix — in 2beebbc6 1v1 (our only 1v1 game), ec_dist increased 211→227 over 9 seconds despite CARRIER_PUSH_DISTANCE_1V1=400 push logic being active (we had 6-9 fighters > threshold=4). Possible causes: (a) enemy carrier is faster or also issuing ATTACK auto-move away from us; (b) FIRE_MISSILE priority 1 consuming cooldown slot every tick the carrier has missiles, blocking MOVE orders for several seconds; (c) border safety kicking in if carrier is near spawn corner. Need game with 1v1 entry at > 400 HP and ≥ 3 active fighters to cleanly observe push behavior. Collect 3 more Klaudiusz games and read threats.csv ec_dist trajectory starting from 1v1_start_s.
