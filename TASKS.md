Execute one of the following tasks, commit and push to github when task is completed.
If no open tasks remain, analyze history/ (.log and .md files only) and add new improvement tasks below.

- [x] Fix fighter missile targeting — fighters fire FIRE_MISSILE when the enemy carrier is within 150 units, but when the fighter is positioned behind or close to our own carrier the missile can lock onto our carrier instead of the enemy. Adjust the fire condition so a fighter only fires when it has line of sight toward the enemy (e.g. the enemy carrier is in the forward half-space relative to the fighter's direction, or the fighter is not between our carrier and the enemy).

- [x] Fix corner approach: reorder CarrierCornerTactic before CarrierBorderEvasionTactic in MultiEnemySurvivalStrategy and MultiEnemyHunterStrategy — carriers spawn near the Y border (y=18 or y=198 in a 216-high world). Border evasion fires first and computes a pure Y offset (0|±62), ignoring X. The corner tactic computes a correct diagonal offset (e.g. -22|-62) pointing directly at the corner. Since the corner is always inside the safe zone (margin=80), border evasion is redundant for the carrier in these strategies and only blocks X correction. Fix: move CarrierCornerTactic to the front of the carrier chain in both multi-enemy strategies.

- [x] Remove CarrierMissileFireTactic from MultiEnemySurvivalStrategy — the FIRE_MISSILE direction vector also moves the carrier toward the target, interfering with corner approach. For spawn (282,18), the nearest enemy is at (102,18); the missile fires LEFT, counteracting the corner tactic's rightward offset. In survival phase we want to reach the corner quickly and stay out of crossfire — firing missiles draws attention and disrupts navigation.

- [x] Remove pre-kill push from MultiEnemyHunterStrategy — the pre-kill push fires whenever an enemy is ≤750 HP, which is almost always true at t=15 when the carrier first reaches the corner (enemies exchange missiles from t=5). The carrier immediately leaves the corner and pushes into a 4-way fight. Analysis: in 10 of 11 games the push triggered at corner arrival and led to death within 30s. Only push after hasKilledEnemy=true, when we have a real numbers advantage. Remove the pre-kill CarrierPushTactic from MultiEnemyHunterStrategy's carrier chain entirely.

- [x] Fix CarrierCornerTactic PATROL overshoot — when the carrier arrives within cornerThreshold the tactic sends PATROL, which maintains the current approach velocity. The carrier overshoots the corner and drifts across the map; subsequent MOVE orders cannot overcome the game's physics momentum before the closest-corner calculation flips to a different corner. Fix: remove PATROL entirely — always send MOVE toward the corner even when within the threshold so the carrier oscillates near the corner rather than escaping it.

- [x] Fix FighterMissileFireTactic friendly-fire — the isBetweenCarrierAndTarget projection check only considers position along the carrier→target axis, not perpendicular offset. A missile auto-homes to the nearest carrier; the real risk is when our carrier is closer to the fighter than the enemy carrier. Replace the projection check with a distance check: skip FIRE_MISSILE if distance(fighter, myCarrier) <= distance(fighter, target).

- [x] Add JUnit 5 unit tests for CarrierCornerTactic and FighterMissileFireTactic — add testImplementation("org.junit.jupiter:junit-jupiter:5.11.4") and testRuntimeOnly("org.junit.platform:junit-platform-launcher") to build.gradle.kts. Write tests covering: (a) CarrierCornerTactic: far from corner → MOVE toward corner, within threshold → small MOVE correction, carrierReachedCorner flag set; (b) FighterMissileFireTactic: enemy closer than carrier → fires, carrier closer than enemy → no fire, no missiles → no fire, enemy out of range → no fire.

- [x] Re-add narrow carrier border evasion in MultiEnemySurvivalStrategy — the carrier spawns at y≈18 and the corner target is now y=20 (CARRIER_CORNER_MARGIN=20). If the carrier overshoots the corner slightly toward y=0 there is no safety net. Add CarrierBorderEvasionTactic with a small margin (~10 units, half of CARRIER_CORNER_MARGIN) after the CarrierCornerTactic in the carrier chain. Keep the value small — the carrier is maneuverable enough to correct a 10-unit drift without the tactic fighting the corner approach.

- [x] Stop fighter recall: replace MOVE 0|0 in BorderEvasionTactic with an inward redirect — BorderEvasionTactic currently sends MOVE 0|0 (return to carrier = dock) whenever a fighter comes within BORDER_MARGIN (50 units) of any wall. Data shows that at t=10 Klaudia has only 3–6 active fighters while opponents have 8–10, and the docked count stays high throughout the game because fighters are constantly recalled, docked, and must be redeployed. Fix: instead of MOVE 0|0, send a MOVE that pushes the fighter toward the carrier with a moderate offset away from the dangerous border — e.g. compute the wall the fighter is near and apply a directional push of BORDER_MARGIN units away from it. This keeps fighters in play rather than cycling through dock/undock.

- [x] Prevent fighter self-destruction from collisions — fighters sent back to the carrier via MOVE 0|0 all converge on the same carrier position and collide, destroying each other. Observed: in game a915c9c8 active fighters drop from 8 to 2 in 3 seconds (t=8→t=11), with destroyed count rising by 5. This happens as multiple fighters simultaneously return. Fix: once the MOVE 0|0 recall is replaced (above task), ensure formation slots spread fighters around the carrier at distinct offsets rather than stacking at 0|0. Review DeploymentDecorator to confirm all docked fighters get distinct target offsets.

- [x] Remove FormationMoveTactic from MultiEnemySurvivalStrategy — FormationMoveTactic pulls any fighter that drifts >100 units from its formation slot back to the carrier on every tick. FighterAttackTactic only runs as a fallback when the fighter is already in formation. The combined effect is that fighters oscillate near the carrier rather than sustaining pressure on enemies: FighterAttackTactic sends a fighter toward an intruder or the primary target, but the next tick FormationMoveTactic recalls it. Data: in every game where ef_near_100 ≥ 7 at t=12, Klaudia died before t=27; in the one win (13611cdc), ef_near_100 was 0. Fix: remove FormationMoveTactic from MultiEnemySurvivalStrategy.fighterTactics. FighterAttackTactic becomes the fallback on every tick, allowing fighters to freely pursue and hold pressure on approaching threats. DeploymentDecorator still uses formation slots on undock so spread-out deployment is preserved.

- [x] Raise intruder intercept range in MultiEnemySurvivalStrategy — FighterAttackTactic currently targets enemy fighters only when they are within FIGHTER_INTRUDER_CARRIER_RANGE = 75f of our carrier. Data shows ef_near_100 ≥ 8 by t=12 in losing games, meaning enemy fighters enter the 100-unit zone before our fighters react. Add FIGHTER_INTRUDER_CARRIER_RANGE_SURVIVAL = 150f to GameConfig and pass it to the FighterAttackTactic in MultiEnemySurvivalStrategy. Fighters will start intercepting approaching threats at 150 units rather than waiting until they breach 75, buying more time to neutralize them before they reach the carrier.

- [x] Fix deployment metering — investigated: the ~1 fighter/1.5s undock rate is a game-engine launch constraint (all players face it; spacecraft_orders shows 43 unique entity IDs — game reassigns IDs on re-launch). Code already correctly orders all docked fighters every tick. Real problem found: FORMATION_EXPAND_AT=8 kept the tight 50-unit radius until 8 fighters were active, putting adjacent fighters only 39 units apart (well within FIGHTER_LASER_RANGE=150). Fixed by lowering FORMATION_EXPAND_AT to 3: with 3+ active fighters the deployment uses the 150-unit wide radius (117-unit gaps), reducing laser-range overlap from tick 1.

- [x] Audit OneVsOneStrategy for the FormationMoveTactic oscillation bug — confirmed: with a dynamic ec/2 formation radius in 1v1, the slot position moves as the carriers converge, creating a moving recall target that interrupts attack runs. Removed FormationMoveTactic from OneVsOneStrategy.fighterTactics; FighterAttackTactic now runs as unconditional fallback, allowing fighters to press to the enemy carrier without being recalled.

- [x] Split fighters into two groups after formation: interceptors and strikers — odd entity-ID suffix = interceptor (FighterAttackTactic with intruder range, falls back to primary target when no intruders), even suffix = striker (FighterAttackTactic with 0 intruder range — always attacks primary carrier). FormationMoveTactic also removed from MultiEnemyHunterStrategy as the same oscillation bug was present there. Applied to both MultiEnemySurvivalStrategy and MultiEnemyHunterStrategy.

- [x] Improve formation slot spread — fighters undock and converge on overlapping formation slots, causing them to cluster tightly around the carrier early in the game. This reduces effective coverage and makes the whole group vulnerable to a single missile or area attack. Review GameConfig formation slot offsets and spacing to ensure fighters are spread across a wider arc; also consider staggering undock timing so fighters don't pile up at the same slot simultaneously.

- [x] Fix missile targeting: fire only when the enemy carrier is the closest carrier to the missile origin — a FIRE_MISSILE order sends the missile toward the target but it homes on the nearest carrier. If our own carrier is closer to the firing fighter than the enemy, the missile curves back. The existing distance check (skip if our carrier is closer than enemy) addresses this for our carrier, but in multi-enemy games another enemy carrier may also be closer. Extend the condition: only fire if the intended target carrier is the closest carrier to the fighter's current position.

- [x] Position strikers in the missile flight path between enemy carrier and their target — missiles home on the nearest carrier. If our strike fighters are positioned between the enemy carrier and our carrier, enemy missiles may intercept our fighters instead of our carrier (fighters act as a shield). Conversely, our own missiles should be fired from a position where our carrier is NOT between the fighter and the enemy. Implement a pre-fire positioning step for the STRIKE group: move to a position on the far side of the enemy carrier (relative to our carrier) before firing.

- [x] Multi-enemy formation: use 180° arc toward primary threat instead of 360° — In `ClaudeCommander.buildSnapshot()`, `formArc` is always `2π` for multi-enemy games, so formation slots spread evenly around the carrier. Half the slots face away from enemies. Data: at t=10 ef_near_100 is already 1–6 in most games, meaning enemy fighters arrive quickly, yet our interceptors may be at slots pointing the wrong way at undock. Fix: change `formArc` to `π` (180°, same as 1v1) even in multi-enemy, centered on `lastFormationAngle` (toward primary target = nearest enemy). This ensures every formation slot and every undock MOVE order faces the threat zone, so fighters intercept incoming enemies rather than drifting away from the fight. The 180° arc with 8 slots still gives 22° separation — plenty of spread. Also addresses user observation: "fighters move to some random place and only after some time they attack."

- [x] 1v1 carrier: hold safe distance by default; only push when clearly winning — `CarrierAttackTactic(hasKilledEnemy)` fires every tick in 1v1 reached from multi-enemy (hasKilledEnemy is always true once we hit 1v1). This sends the carrier rushing at the enemy unconditionally. `CarrierPushTactic(400, hasKilledEnemy && fighters≥4)` then charges 400 units whenever conditions hold. In game 039acc5c we entered 1v1 at 795 HP vs enemy 1000 HP (disadvantage) and still rushed — wrong. Fix in two parts: (1) Default behavior: maintain ~250-unit separation from the enemy carrier using a distance-hold MOVE (not patrol), letting fighters do damage. (2) Aggressive push only when: own_hp − enemy_hp > 300 AND own_active_fighters > enemy estimated fighters × 1.5. Replace CarrierAttackTactic with a conditioned version; update CarrierPushTactic condition accordingly. Do not rush if at a health or fighter disadvantage.

- [x] 1v1 endgame: all-out carrier ram when overwhelmingly superior — separate from the push condition above, implement a true "ram" trigger: if own_hp > 600 AND enemy_hp < 400 AND own_active_fighters ≥ 6, override the distance-hold and drive the carrier directly at the enemy. This is the "safely ram it" case: we are at full health, enemy is badly wounded, and we have more than enough fighters to suppress incoming missiles. Use CarrierPushTactic or a new CarrierRamTactic inserted before the distance-hold in the 1v1 carrier chain.

- [x] Investigate corner approach failure and add first-tick guarantee — At least one game (4c67b516, all_carriers confirms carrier moved from (282,18) to (338,159) over 32 s — direction is 93% vertical, not toward the top-right corner at (364,20)). The correct MOVE from (282,18) is (+82,+2) = almost purely horizontal, yet the carrier moved mostly downward. Root cause is unknown without spacecraft_orders for that game. Tasks: (a) re-run a game with spacecraft_orders enabled for the carrier entity and check the first few MOVE offsets; (b) ensure `ClaudeCommander.process()` fires on the very first server tick so no undirected initial velocity is allowed to carry the carrier in the wrong direction; (c) if the wrong corner is being chosen (e.g. bottom-right instead of top-right from (282,18)), add an assertion or log to verify closestCornerOffset outputs match expectations.

- [x] Remove StrikerPositioningTactic — it blocks attack orders and causes fighter collisions. Root cause: at the overshoot point (40 units past enemy carrier), `distToTarget = 40` and `distToCarrier = d + 40` (where d is the carrier→target distance, typically ≥150 units), so the "far side" condition `distToTarget < distToCarrier` is never satisfied. StrikerPositioningTactic fires every tick, preventing FighterMissileFireTactic and FighterAttackTactic from running. Strikers park near the enemy carrier without firing or attacking. Additionally, all strikers receive the same carrier-relative MOVE offset (dx+ndx*40 | dy+ndy*40), so they converge on the same world coordinate and collide. Evidence: in game dbead326 (win), carrier reached corner at t=20 with 10 active fighters yet nearest enemy HP only dropped 750→595 over 13 seconds (very slow). Fix: delete `StrikerPositioningTactic.java`, remove it from `MultiEnemySurvivalStrategy.strikerTactics` and `MultiEnemyHunterStrategy.strikerTactics`, and remove `STRIKER_POSITION_RANGE` from `GameConfig`. The friendly-fire guard already in `FighterMissileFireTactic` (skip if any carrier closer than target) is sufficient protection. `FighterAttackTactic(0f)` then becomes the striker's continuous fallback, sending ATTACK to the primary target every tick.

- [x] Improve missile interception — expand detection range, persist assignments, use midpoint targeting. Evidence: min_enemy_missile_dist_to_our_carrier = 10.5–10.6 in all 3 analyzed games; missiles reach the carrier in every game. Root cause: `MISSILE_INTERCEPT_RANGE = 80f` is too short — by the time a missile enters 80 units there is no time for a fighter to reposition. Additionally `buildInterceptMap` reassigns the "closest fighter" each tick, causing oscillation instead of committed interception. Fix: (1) Raise `MISSILE_INTERCEPT_RANGE` from 80f to 150f in `GameConfig`. (2) Add `Map<String, String> missileInterceptAssignments = new HashMap<>()` to `CommanderState` (key = missileId, value = fighterId). (3) Refactor `ClaudeCommander.buildInterceptMap()`: for each threatening missile within 150 units of our carrier, re-use the existing assignment if the assigned fighter is still active; otherwise assign the active fighter closest to our carrier that is not already assigned. Remove stale entries (missile gone or fighter gone). (4) Change the intercept target from the missile's current position to the midpoint between missile and carrier: `interceptOffset = ((missile.px − carrier.px) / 2, (missile.py − carrier.py) / 2)` — this places the fighter on the missile's flight path. (5) Store the result in `state.missileInterceptAssignments` AND in `GameSnapshot.interceptMap()` (format unchanged: carrier-relative int offset). `MissileInterceptTactic` needs no changes — it already sends MOVE to the carrier-relative offset. The existing `LaserDefenseTactic` fires `TARGET "M"` automatically once the intercepting fighter closes to within 150 units of the missile.

- [x] Add anti-missile missile fire for assigned interceptors — verified in client source and spacecraft_orders logs: `FIRE_MISSILE` only supports `details=null` (auto-home to nearest carrier) or a direction vector `"dx|dy"` (carrier variant). No entity-ID targeting is supported. Attempting to pass a missile entity ID as details would either be ignored or home on the nearest carrier. The intercept chain already handles the problem: `MissileInterceptTactic` positions the fighter on the missile's path, `LaserDefenseTactic` fires `TARGET "M"` (laser) once the missile closes to within 150 units, and physical collision rams the missile as a last resort. No new tactic implemented; API limitation documented.

- [x] Fix MultiEnemyHunterStrategy carrier chain — unblock CarrierMissileFireTactic and CarrierBorderEvasionTactic. Root cause: `CarrierCornerTactic` is position 1 in the carrier chain and always returns a non-empty Optional, permanently blocking every tactic below it. Consequence: (a) `CarrierMissileFireTactic` at position 3 never fires — the carrier never uses its 4 missiles in hunter phase (confirmed: no FIRE_MISSILE in spacecraft_orders for ORANGE-00 in any multi-enemy game); (b) `CarrierBorderEvasionTactic` at position 2 never fires — carrier slides along borders unchecked; (c) `CarrierPushTactic` at position 4 never fires. Fix: reorder the chain to: `[CarrierBorderEvasionTactic(BORDER_MARGIN, SAFE_INSET), CarrierMissileFireTactic(CARRIER_MISSILE_FIRE_INTERVAL_MS), CarrierPushTactic(CARRIER_PUSH_DISTANCE, pushCondition), CarrierCornerTactic(CARRIER_CORNER_MARGIN, CARRIER_CORNER_THRESHOLD)]`. Remove the duplicate second `CarrierCornerTactic` at the original position 5. CarrierMissileFireTactic only fires when the interval elapses and returns FIRE_MISSILE (no MOVE), leaving the carrier with its physics momentum for that tick — this is fine.

- [x] Fix carrier border-sliding — increase border evasion trigger margin and inset in survival phase. Evidence: games 9c90614a and 710f9070 both show the carrier sliding from x≈20–28 down to x=0.1 along the left border over 10–14 seconds. Root cause: `CarrierBorderEvasionTactic(CARRIER_BORDER_SAFETY=10f, safeInset=0f)` triggers only at x<10 and computes push offset as `safeX − carrier.px = 10 − 8.4 = 1.6` — a 2-unit nudge that physics momentum easily overrides each tick. The corner target is x=20 (`CARRIER_CORNER_MARGIN`), so by the time border evasion fires (x<10) the carrier has already overshot 10 units past the corner. Fix: in `MultiEnemySurvivalStrategy`, change `CarrierBorderEvasionTactic(CARRIER_BORDER_SAFETY, 0f)` to `CarrierBorderEvasionTactic(CARRIER_CORNER_MARGIN, CARRIER_BORDER_SAFETY_INSET)` where `CARRIER_CORNER_MARGIN = 20f` (trigger exactly at the corner boundary) and `CARRIER_BORDER_SAFETY_INSET = 10f` (push to 30 units from border — past the corner with a cushion). Add `CARRIER_BORDER_SAFETY_INSET = 10f` to `GameConfig`. Remove `CARRIER_BORDER_SAFETY`. Also apply the same fix in `MultiEnemyHunterStrategy` (after fixing its chain ordering in the task above), replacing `(BORDER_MARGIN=50, SAFE_INSET=30)` with the tighter corner-aligned values.

- [x] Distribute striker attacks across all live enemy carriers — currently all strikers (even entity-ID suffix) attack `snapshot.primaryTarget()`, a single carrier. In game 74df428b, ORANGE survived 32 seconds with 1000 HP completely unattacked while all 10–14 of our fighters attacked BLUE — ORANGE was killed by other players, not us. Fix: create `DistributedAttackTactic` (placed where `FighterAttackTactic(0f)` currently sits in striker chains). In `apply()`: get `liveEnemyCarriers` sorted by distance to our carrier; parse `fighterIdSuffix = Integer.parseInt(fighter.id().split("-")[last])` (same pattern as `isInterceptor`); pick `carriers.get(fighterIdSuffix % carriers.size())` as the individual striker's target; send `ATTACK targetId`. When a carrier dies, `liveEnemyCarriers` shrinks and the modulo naturally redistributes fighters. Replace `FighterAttackTactic(0f)` for strikers in both `MultiEnemySurvivalStrategy` and `MultiEnemyHunterStrategy`. Interceptors keep `FighterAttackTactic(intruderRange)` unchanged — they focus on intruders and the global primary target. `OneVsOneStrategy` is unaffected (single carrier, no distribution needed).

- [x] Flip deployment direction away from border — fighters undock on the border side of the carrier. Root cause: `deployAngle` defaults to `lastFormationAngle` (toward the primary target), but when the carrier is near a corner the target angle points partially toward a border. Half the 180° arc lands in the border zone, so newly-undocked fighters trigger `BorderEvasionTactic` immediately instead of attacking. The existing `ENEMY_CLOSE_DEPLOY_THRESHOLD` flip already handles the "enemy too close" case by testing the center slot; apply the same logic for borders. Fix: in `buildSnapshot()`, after the enemy-close flip, compute the center slot of the current `deployAngle`: `slotX = myCarrier.px() + formRadius * cos(deployAngle)`, `slotY = myCarrier.py() + formRadius * sin(deployAngle)`. If `isNearBorder(slotX, slotY, settings, BORDER_MARGIN)`, try `deployAngle + π` — only use it if the flipped center slot is not near the border. Add `DEPLOY_BORDER_MARGIN = BORDER_MARGIN` to `GameConfig` for the check (or reuse `BORDER_MARGIN` directly). Apply this border-flip after the enemy-close flip so both conditions can cooperate.

- [x] Fire fighter missiles aggressively — fire whenever enemy carrier is in front and our carrier is not in the trajectory. Root cause: `FighterMissileFireTactic` blocks fire if any OTHER enemy carrier is closer than `primaryTarget`. In 4-player games carriers are equidistant so this almost always fires, resulting in near-zero missile usage. Fighters also die before firing because the conditions are too strict. Fix: rewrite the fire condition to be: fire `FIRE_MISSILE` (auto-home) when (1) any live enemy carrier exists, (2) the nearest enemy carrier is at least `FIGHTER_MISSILE_MIN_FIRE_RANGE` away, AND (3) our carrier is not in the missile's trajectory. Condition 3 uses a dot-product + distance check: our carrier is "in the way" only if it is both in the same forward hemisphere as the nearest enemy (`dot(normalize(nearestEnemy − fighter), normalize(myCarrier − fighter)) > 0`) AND closer to the fighter than that enemy. If the carrier is to the side or behind the fighter, it is not in the way and fire is allowed. Remove the `primaryTarget` field usage and the per-carrier loop entirely. Worst case the missile hits an enemy fighter or missile on the way — that is also a win. Update `FighterMissileFireTacticTest`: (a) enemy in front at safe range, carrier behind → fires; (b) carrier in front and closer than enemy → blocked; (c) carrier to the side, enemy in front → fires; (d) nearest enemy within arming range → blocked; (e) no live enemies → blocked.

- [x] Lower `FIGHTER_MISSILE_MIN_FIRE_RANGE` — missile fire never happens in practice because the arming-range threshold equals the formation radius. Fighters are deployed at `FORMATION_RADIUS_WIDE = 150` from the carrier, directly toward the nearest enemy. In a 384×216 four-player world enemies are 150–260 units from the carrier, so fighters land 0–110 units from the nearest enemy. The `distToNearest < 150` check blocks every shot. The minimum range was set equal to the formation radius — they cancel each other out. Fix: lower `FIGHTER_MISSILE_MIN_FIRE_RANGE` in `GameConfig` from 150 to 100. At 100, fighters firing from their formation position (~150 from carrier) can hit enemies up to 250 units away; fighters that have advanced further can fire at anything > 100 units. The missile may occasionally fire from just under arming distance, but it overshoots, curves back (auto-homing), and still arms before returning — acceptable trade-off vs. never firing at all. Update `FighterMissileFireTacticTest` case (d) `nearestEnemyWithinArmingRange_doesNotFire` to use an enemy within the new 100-unit threshold. Also update `CARRIER_MISSILE_MIN_FIRE_RANGE` to 100 for consistency (same arming physics, same fix).

- [x] Cap missile-intercept assignments so fighters keep attacking — `buildInterceptMap` assigns one fighter per inbound missile and the assignments are sticky for the missile's lifetime. With 3–5 missiles in flight (common after t=10), 3–5 fighters are continuously sent `MOVE` to the carrier zone and never attack. There is no upper bound. Fix: add `MISSILE_INTERCEPT_MAX_FIGHTERS = 2` to `GameConfig`. In `buildInterceptMap`, stop assigning once `result.size() >= MISSILE_INTERCEPT_MAX_FIGHTERS` — but keep existing assignments that are already in `state.missileInterceptAssignments` (honour committed fighters). This ensures at most 2 fighters defend at a time while the rest press the attack.

- [x] Fix deployment slots that fall outside the safe zone — when the carrier is in a corner the 180° deployment arc produces slots that land outside or very near the world boundary. Example: carrier at (359, 20), formation angle 152°, slot at 62° computes to world (429, 153) — outside the right border. The center-slot border-flip only checks the center; extreme slots go unchecked. Fighters sent to these slots immediately trigger `BorderEvasionTactic` (the carrier at x=359 is already in the border margin) and oscillate near the carrier for several seconds instead of attacking. Fix: in `buildSnapshot()`, after building `deploymentFormation`, iterate all slots: compute the world position (`carrier.px + slot[0]`, `carrier.py + slot[1]`); if it is outside `[BORDER_MARGIN, worldWidth-BORDER_MARGIN] × [BORDER_MARGIN, worldHeight-BORDER_MARGIN]`, push the slot along its current angle to the safe-zone boundary. Concretely: normalise the carrier→slot direction, then find the largest scale factor ≤ `formRadius` such that the world position stays inside the safe box; use that as the clamped offset. Multiple slots may collapse to the same clamped position — that is acceptable (some fighters share a slot) but add a small distinct offset per duplicate if needed.

- [x] Remove or restrict `LaserDefenseTactic` — it preempts `FighterAttackTactic` on every tick that any missile is within 150 units of a fighter, consuming the order slot and preventing advance. In a typical missile volley all fighters within 150 units of any missile switch to `TARGET "M"` instead of attacking. Root cause: the range check is global (any missile anywhere), not targeted at missiles threatening OUR carrier. Fix: restrict `LaserDefenseTactic` to fire only when a missile is within `laserRange` of the fighter AND its velocity vector points within 60° of our carrier (i.e., it is heading toward our carrier, not toward someone else). To check: `dot(normalize(missile.velocity), normalize(myCarrier − missile.position)) > cos(60°) = 0.5`. If the game does not provide useful velocity data, remove `LaserDefenseTactic` entirely — `MissileInterceptTactic` already handles physical interception and the laser defense was not measurably helping (no evidence in history of missiles being destroyed by fighters using `TARGET "M"`).

- [x] Reorder fighter priority chain: orient then fire missiles, then intercept, then enemy fighters, then advance — current chain puts `MissileInterceptTactic` before `FighterMissileFireTactic`, so assigned interceptors are immediately redirected to defend and never fire their missiles. The interceptor/striker split also sends half the squad to defend missiles full-time rather than attacking. Desired priority (same for all fighters, no interceptor/striker split): (1) **Orient and fire missiles** — while the fighter still has missiles, the sole goal is to fire them. Missiles launch in the direction the fighter is currently flying, so the fighter must be pointing at the enemy carrier first. Each tick: check if the fighter's velocity vector is within 45° of the nearest enemy carrier (`dot(normalize(vx,vy), normalize(target−fighter)) > cos(45°) ≈ 0.707`). If not yet oriented, issue `ATTACK targetCarrierId` (which moves the fighter toward the target and builds up velocity in that direction). If already oriented AND the fire conditions pass (enemy carrier ≥ `FIGHTER_MISSILE_MIN_FIRE_RANGE`, no friendly-fire trajectory), issue `FIRE_MISSILE`. If fighter is stationary (speed < 0.1), treat as not oriented. This replaces `FighterMissileFireTactic` entirely — implement as a new `FighterMissileOrientAndFireTactic`. (2) **Intercept assigned missile** — `MissileInterceptTactic` runs only when `fighter.missiles() == 0` AND the fighter has an intercept assignment; fighters with remaining missiles keep attacking. (3) **Attack nearby enemy fighter** — `FighterAttackTactic(FIGHTER_INTRUDER_CARRIER_RANGE)`: if an enemy fighter is within `FIGHTER_INTRUDER_CARRIER_RANGE` of our carrier, intercept it. (4) **Advance to laser range and attack enemy carrier** — `DistributedAttackTactic` as the final fallback; fighter issues `ATTACK` toward the nearest enemy carrier, moving closer each tick until in laser range. `LaserDefenseTactic` stays as a passive guard but placed last (already restricted to carrier-directed missiles). Implementation: remove the even/odd split (`isInterceptor`) from all strategies — every fighter gets the same single chain. Apply to `MultiEnemySurvivalStrategy`, `MultiEnemyHunterStrategy`, and `OneVsOneStrategy`.

- [x] Add MultiEnemyControlStrategy: exit Survival when target corner area is empty (no enemy within 90 units of either corner border); new Control strategy cruises the carrier slowly along the free horizontal border, focuses all fighter and missile attacks on a single selected enemy (strongest or closest, with priority to whoever is attacking us), and exits to Hunter when an enemy is destroyed or the carrier traverses 3/4 of the border.
